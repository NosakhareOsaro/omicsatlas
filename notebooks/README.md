# notebooks

Exploratory notebooks only — **not for production use**. Nothing here is imported by
`src/omicsatlas` or the pipelines; treat these as scratch analysis that may become stale.
Any logic worth keeping belongs in `src/omicsatlas/<module>` with tests, not here.
