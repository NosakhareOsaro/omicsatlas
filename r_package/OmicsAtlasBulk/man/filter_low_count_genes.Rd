% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_data.R
\name{filter_low_count_genes}
\alias{filter_low_count_genes}
\title{Filter out genes with very low total counts}
\usage{
filter_low_count_genes(se, min_total_count = 10, assay_name = "counts")
}
\arguments{
\item{se}{A \code{SummarizedExperiment} with a raw-count assay.}

\item{min_total_count}{Minimum summed count (across all samples) for a gene to
be kept. Default \code{10}.}

\item{assay_name}{Which assay in \code{se} holds counts. Default \code{"counts"}.}
}
\value{
\code{se}, subset to the genes passing the filter.
}
\description{
A standard pre-filtering step recommended before DESeq2/edgeR (both packages'
own vignettes suggest it): drops genes whose counts summed across all samples
fall below \code{min_total_count}, before either method's own internal
independent-filtering runs. Mainly useful for the TCGA-BRCA subset, whose
~60,660-feature GENCODE annotation includes many pseudogenes/lncRNAs with
near-zero counts across the whole cohort.
}
\examples{
data(example_bulk_se)
filter_low_count_genes(example_bulk_se, min_total_count = 20)
}
