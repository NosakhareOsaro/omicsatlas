% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_de.R
\name{extract_fold_change}
\alias{extract_fold_change}
\title{Extract the fold-change column from a DESeq2 or edgeR result}
\usage{
extract_fold_change(res)
}
\arguments{
\item{res}{A \code{\link[=run_deseq2]{run_deseq2()}} or \code{\link[=run_edger]{run_edger()}} result (or any data frame/DataFrame
with a \code{log2FoldChange} or \code{logFC} column).}
}
\value{
The fold-change column, as given (whatever numeric type/class \code{res}
itself uses for its columns).
}
\description{
\code{\link[=run_deseq2]{run_deseq2()}} results have a \code{log2FoldChange} column; \code{\link[=run_edger]{run_edger()}} results have
\code{logFC} instead. Callers that need "the fold-change column, whichever DE method
this came from" (e.g. building a ranked gene list for GSEA) should use this
rather than hardcoding one column name — a real incident (see \code{ADR-0006})
silently produced \code{NULL} for the un-hardcoded method by doing exactly that.
Errors explicitly if neither column is present, rather than returning \code{NULL} for
some future third DE method with yet another naming convention.
}
\examples{
data(example_bulk_se)
deseq2_res <- run_deseq2(example_bulk_se, condition_column = "condition")
edger_res <- run_edger(example_bulk_se, condition_column = "condition")
extract_fold_change(deseq2_res)
extract_fold_change(edger_res)
}
