% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment.R
\name{run_enrichment}
\alias{run_enrichment}
\title{Run GO, KEGG, and GSEA functional enrichment}
\usage{
run_enrichment(
  gene_symbols,
  ranked_stats,
  ont = "BP",
  organism_db = org.Hs.eg.db::org.Hs.eg.db,
  pvalue_cutoff = 0.05,
  run_kegg = TRUE
)
}
\arguments{
\item{gene_symbols}{Character vector of significant gene symbols (used for GO/KEGG
over-representation).}

\item{ranked_stats}{Named numeric vector, gene symbol -> ranking statistic
(typically \code{log2FoldChange}/\code{logFC}), covering every \emph{tested} gene (not just the
significant ones) — used for GSEA.}

\item{ont}{GO ontology: \code{"BP"}, \code{"MF"}, \code{"CC"}, or \code{"ALL"}. Default \code{"BP"}.}

\item{organism_db}{An \code{OrgDb} annotation object. Default
\code{org.Hs.eg.db::org.Hs.eg.db}.}

\item{pvalue_cutoff}{p-value cutoff for the GO/KEGG over-representation tests.
Default \code{0.05}. GSEA is run with no cutoff (\code{1}) so callers can inspect/filter
the full ranked result themselves.}

\item{run_kegg}{If \code{FALSE}, skip the network-dependent KEGG step entirely and
return \code{kegg = NULL}. Default \code{TRUE}.}
}
\value{
A list with \code{go}, \code{kegg}, and \code{gsea} elements (each a clusterProfiler
\code{enrichResult}/\code{gseaResult}, or \code{NULL} if that step had no input or was
skipped).

The KEGG step is wrapped in \code{\link[=retry_with_backoff]{retry_with_backoff()}} - KEGG's REST API has no
documented rate limit but is known to be occasionally flaky/rate-limited under
repeated calls (observed directly during this project's real TCGA-BRCA pipeline
run: 3 calls in quick succession, one outright failure that a bare retry of the
identical call then succeeded at - see \code{ADR-0005}).
}
\description{
Runs over-representation analysis (GO, KEGG) on a significant-gene set and gene
set enrichment analysis (GSEA, on GO terms) on a full ranked gene list, via
\code{clusterProfiler}. Intended to run once per DE method's significant-gene set (see
\code{\link[=run_deseq2]{run_deseq2()}}/\code{\link[=run_edger]{run_edger()}}/\code{\link[=compare_de_methods]{compare_de_methods()}}) so results from both methods
can be reported side by side rather than only enriching one method's output.
}
\details{
GO enrichment (\code{enrichGO}/\code{gseGO}) uses the local \code{OrgDb} annotation and needs no
network access. \strong{KEGG enrichment (\code{enrichKEGG}) calls KEGG's REST API and
genuinely requires live network access} — clusterProfiler has no offline KEGG
data source. This package's own test suite exercises the GO/GSEA paths for real
but skips the KEGG path in CI (\code{testthat::skip_on_ci()}) rather than either
silently mocking it or letting a CI run depend on an external service; KEGG
enrichment is validated for real only in the actual pipeline run against the
project's real bulk RNA-seq data (see \code{docs/bulk-pipeline.md}).
}
\examples{
data(example_bulk_se)
res <- run_deseq2(example_bulk_se, condition_column = "condition")
sig_genes <- SummarizedExperiment::rowData(example_bulk_se)$gene_symbol[res$is_significant]
ranked <- stats::setNames(
  res$log2FoldChange,
  SummarizedExperiment::rowData(example_bulk_se)$gene_symbol
)
# KEGG needs live network access; skipped here so this example runs offline.
enrichment <- run_enrichment(sig_genes, ranked, run_kegg = FALSE)
enrichment$go
}
