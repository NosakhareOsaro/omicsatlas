% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_data.R
\name{read_tcga_brca_counts}
\alias{read_tcga_brca_counts}
\title{Load the TCGA-BRCA STAR-Counts subset into a SummarizedExperiment}
\usage{
read_tcga_brca_counts(
  manifest_path,
  repo_root = dirname(dirname(manifest_path))
)
}
\arguments{
\item{manifest_path}{Path to the fetch manifest JSON, e.g.
\code{data/.provenance/tcga_brca_subset.json}.}

\item{repo_root}{Root directory the manifest's \code{dest} paths (e.g.
\code{data/raw/bulk/tcga_brca/...}) are relative to. Default: two directories up
from \code{manifest_path} (right for the manifest's real
\code{data/.provenance/tcga_brca_subset.json} location - the repository root -
but overridable, e.g. for tests using a fixture manifest elsewhere).}
}
\value{
A \code{SummarizedExperiment} with a \code{counts} assay (genes x samples),
\code{rowData$gene_symbol}, and \code{colData} columns \code{case_submitter_id} and
\code{sample_type} (\code{"Primary Tumor"}/\code{"Solid Tissue Normal"} - GDC's own values,
suitable directly as a \code{\link[=run_deseq2]{run_deseq2()}}/\code{\link[=run_edger]{run_edger()}} \code{condition_column}).
}
\description{
Reads the per-sample GDC STAR-Counts TSVs referenced by the fetch manifest
\code{scripts/fetch_data.py} wrote (\code{tcga_brca_subset} - see \code{ADR-0003}), using each
file's \code{unstranded} column as the raw count (GDC's recommended default when
library strandedness isn't otherwise pinned down). Gene rows are keyed by
Ensembl gene ID with its version suffix stripped (\code{gene_name}/symbol is not
unique across all ~60k GENCODE features in this file - 110 duplicated symbols in
the fetched subset, checked directly); \code{rowData$gene_symbol} carries \code{gene_name}
for \code{\link[=run_enrichment]{run_enrichment()}}, duplicates and all.
}
\examples{
\donttest{
# Requires the real fetched files (scripts/fetch_data.py tcga_brca_subset).
se <- read_tcga_brca_counts("data/.provenance/tcga_brca_subset.json")
}
}
