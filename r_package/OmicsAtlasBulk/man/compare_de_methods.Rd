% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_de.R
\name{compare_de_methods}
\alias{compare_de_methods}
\title{Compare DESeq2 and edgeR differential expression results}
\usage{
compare_de_methods(deseq2_res, edger_res)
}
\arguments{
\item{deseq2_res}{Result of \code{\link[=run_deseq2]{run_deseq2()}}.}

\item{edger_res}{Result of \code{\link[=run_edger]{run_edger()}}, on the same data.}
}
\value{
A list with:
\describe{
\item{overlap}{List of gene-ID character vectors: \code{both}, \code{deseq2_only},
\code{edger_only}.}
\item{venn_plot}{A \code{ggplot} Venn diagram of the two significant-gene sets.}
\item{upset_matrix}{A binary membership matrix (\code{UpSetR::fromList()} output),
renderable via \code{UpSetR::upset()}.}
\item{discrepancy}{Data frame, one row per gene significant in exactly one
method, with each method's \code{log2FoldChange}/\code{logFC} and significance flag
side by side.}
}
}
\description{
Compares the significant-gene sets from \code{\link[=run_deseq2]{run_deseq2()}} and \code{\link[=run_edger]{run_edger()}} run on
the same data, producing an overlap summary, Venn and UpSet-style
visualisations, and an explicit discrepancy table — genes exactly one method
calls significant — rather than silently picking one method's result as "the"
answer. \code{deseq2_res} and \code{edger_res} must come from the same
\code{SummarizedExperiment} (same genes, same row order).
}
\examples{
data(example_bulk_se)
deseq2_res <- run_deseq2(example_bulk_se, condition_column = "condition")
edger_res <- run_edger(example_bulk_se, condition_column = "condition")
comparison <- compare_de_methods(deseq2_res, edger_res)
comparison$overlap
}
