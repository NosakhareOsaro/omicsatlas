% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/align.R
\name{run_star_align}
\alias{run_star_align}
\title{Run STAR alignment}
\usage{
run_star_align(
  fastq_files,
  genome_dir,
  output_prefix,
  star_path = "STAR",
  extra_args = character(),
  dry_run = FALSE
)
}
\arguments{
\item{fastq_files}{Character vector of 1 (single-end) or 2 (paired-end) FASTQ
file paths.}

\item{genome_dir}{Path to a STAR genome index directory (built with
\verb{STAR --runMode genomeGenerate}).}

\item{output_prefix}{Path prefix for STAR's output files (STAR appends its own
suffixes, e.g. \code{Aligned.sortedByCoord.out.bam}).}

\item{star_path}{Path to the STAR executable. Defaults to \code{"STAR"}, resolved via
\code{PATH}.}

\item{extra_args}{Character vector of additional STAR command-line arguments,
appended after the standard ones.}

\item{dry_run}{If \code{TRUE}, construct and return the command-line arguments without
running STAR — used by tests and by the example below, which don't assume STAR
is installed.}
}
\value{
If \code{dry_run = TRUE}, the constructed character vector of STAR arguments
(invisibly). Otherwise, the path to the aligned, coordinate-sorted BAM file STAR
produces (\verb{<output_prefix>Aligned.sortedByCoord.out.bam}), invisibly; errors if
STAR exits with a non-zero status.
}
\description{
Wraps a real STAR command-line invocation for RNA-seq read alignment against a
pre-built genome index. This is a genuine wrapper around the external \code{STAR}
binary, not a reimplementation — see \code{ADR-0004} in the parent repository for why
STAR stays external-binary-wrapped (rather than, say, \code{Rsubread::align()}), and
for why this function is only tested against a tiny synthetic FASTQ + reference
fixture: neither of this project's two approved bulk RNA-seq datasets
(\code{ADR-0003}) has practically available raw FASTQs to align for real.
}
\examples{
# Construct (but do not run) a STAR command line, using the package's bundled toy
# paired-end FASTQ fixture:
r1 <- system.file("extdata", "toy_reads_R1.fastq.gz", package = "OmicsAtlasBulk")
r2 <- system.file("extdata", "toy_reads_R2.fastq.gz", package = "OmicsAtlasBulk")
run_star_align(
  fastq_files = c(r1, r2),
  genome_dir = tempdir(),
  output_prefix = file.path(tempdir(), "toy_"),
  dry_run = TRUE
)
}
