% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{render_bulk_report}
\alias{render_bulk_report}
\title{Render an auto-generated Quarto HTML report from bulk pipeline outputs}
\usage{
render_bulk_report(
  deseq2_res,
  edger_res,
  comparison,
  enrichment_deseq2 = NULL,
  enrichment_edger = NULL,
  deconvolution_props = NULL,
  dataset_label,
  output_file,
  quiet = TRUE
)
}
\arguments{
\item{deseq2_res}{Result of \code{\link[=run_deseq2]{run_deseq2()}}.}

\item{edger_res}{Result of \code{\link[=run_edger]{run_edger()}}, on the same data.}

\item{comparison}{Result of \code{compare_de_methods(deseq2_res, edger_res)}.}

\item{enrichment_deseq2}{Result of \code{\link[=run_enrichment]{run_enrichment()}} on DESeq2's significant
genes (a list with \code{go}/\code{kegg}/\code{gsea}), or \code{NULL} to omit that section.}

\item{enrichment_edger}{Result of \code{\link[=run_enrichment]{run_enrichment()}} on edgeR's significant
genes, or \code{NULL} to omit that section.}

\item{deconvolution_props}{Result of \code{\link[=run_bayesprism_deconvolution]{run_bayesprism_deconvolution()}} (a
samples-by-cell-type proportion matrix), or \code{NULL} to omit that section.}

\item{dataset_label}{Short human-readable label for the dataset this report
covers (e.g. \code{"GSE176078-matched bulk (primary)"}), used in the report title.}

\item{output_file}{Path to write the rendered HTML report to.}

\item{quiet}{Passed to \code{quarto::quarto_render()}. Default \code{TRUE}.}
}
\value{
\code{output_file}, invisibly.
}
\description{
Renders a full HTML report - a differential expression summary and top genes for
both methods, the DESeq2/edgeR overlap comparison (Venn diagram, discrepancy
table), functional enrichment results, and (optionally) BayesPrism deconvolution
proportions - entirely from the R objects the pipeline itself already produced
(\code{\link[=run_deseq2]{run_deseq2()}}, \code{\link[=run_edger]{run_edger()}}, \code{\link[=compare_de_methods]{compare_de_methods()}}, \code{\link[=run_enrichment]{run_enrichment()}},
\code{\link[=run_bayesprism_deconvolution]{run_bayesprism_deconvolution()}}). The bundled Quarto template
(\code{inst/quarto/bulk_report.qmd}) has no hardcoded numbers or narrative baked in -
every table, count, and plot it shows is computed live from whatever objects are
passed in here, so re-running this against fresh pipeline output can never leave
stale numbers behind.
}
\details{
Requires the Quarto CLI to be installed and on \code{PATH} (checked via
\code{quarto::quarto_available()}); the \code{quarto} R package is a \code{Suggests} dependency
rather than \code{Imports} since it wraps an external CLI this package doesn't itself
install, following the standard R packaging convention for that case.
}
\examples{
\donttest{
# Requires the Quarto CLI to be installed; see tests/testthat/test-report.R for a
# complete, real, executed run on the bundled example_bulk_se fixture.
data(example_bulk_se)
deseq2_res <- run_deseq2(example_bulk_se, condition_column = "condition")
edger_res <- run_edger(example_bulk_se, condition_column = "condition")
comparison <- compare_de_methods(deseq2_res, edger_res)
render_bulk_report(
  deseq2_res, edger_res, comparison,
  dataset_label = "example_bulk_se",
  output_file = tempfile(fileext = ".html")
)
}
}
