% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deconvolution.R
\name{run_bayesprism_deconvolution}
\alias{run_bayesprism_deconvolution}
\title{Run BayesPrism deconvolution against the OmicsAtlas scRNA-seq signature}
\usage{
run_bayesprism_deconvolution(
  bulk_se,
  reference_sce,
  assay_name = "counts",
  cell_type_col = "singler_label",
  cell_state_col = "leiden",
  n_cores = 1,
  ...
)
}
\arguments{
\item{bulk_se}{\code{SummarizedExperiment} with a raw-count assay (bulk RNA-seq
samples).}

\item{reference_sce}{A \code{SingleCellExperiment} loaded from the Phase 1 scRNA-seq
signature artifact. Its default assay is used as the reference counts (the
artifact's \code{.X}, i.e. QC-passed raw counts — \emph{not} \code{scran_normalized}; BayesPrism
wants raw counts).}

\item{assay_name}{Which assay in \code{bulk_se} holds raw counts. Default \code{"counts"}.}

\item{cell_type_col}{\code{colData} column in \code{reference_sce} giving cell-type labels.
Default \code{"singler_label"} (the Phase 1 signature's SingleR annotation column —
see \code{DATA_SCHEMA.md}).}

\item{cell_state_col}{\code{colData} column in \code{reference_sce} giving raw cell-state
labels (a finer-grained tier within each type, as BayesPrism recommends).
Default \code{"leiden"} (the Phase 1 signature's cluster column). The actual state
label passed to BayesPrism is \code{paste(cell_type, raw_state)}, not this column's
raw values directly - BayesPrism requires states to nest within types, and
\code{leiden} clusters (computed independently of \code{singler_label}) don't nest on
their own (verified on the real signature: a single Leiden cluster spans
nearly every SingleR type). Combining the two guarantees valid nesting by
construction, for any \code{cell_type_col}/\code{cell_state_col} pair, without reshaping
the underlying artifact.}

\item{n_cores}{Passed to \code{BayesPrism::run.prism()}. Default \code{1}.}

\item{...}{Additional arguments passed to \code{BayesPrism::new.prism()} (e.g.
\code{outlier.cut}, \code{outlier.fraction}).}
}
\value{
A samples-by-cell-type matrix of estimated proportions
(\code{BayesPrism::get.fraction(..., state.or.type = "type")}).

No \code{key} (tumour-reference) is passed to \code{BayesPrism::new.prism()} — the Phase 1
signature's SingleR reference has no malignant/normal-epithelial split to key on
(a documented limitation carried from \code{ADR-0002}/\code{DATA_SCHEMA.md}), so this runs
BayesPrism's plain cell-type/cell-state mode rather than inventing a malignant key.
}
\description{
Deconvolves bulk RNA-seq samples into cell-type proportions using BayesPrism
(installed from GitHub, pinned to a specific commit — see \code{ADR-0004}), with the
reference built from the exact, versioned Phase 1 scRNA-seq signature artifact
(see the parent repository's \code{DATA_SCHEMA.md} and
\code{src/omicsatlas/scrna/artifact.py}'s \code{signature_path()} contract) — never
recomputed or approximated here.
}
\details{
\code{reference_sce} takes an already-loaded \code{SingleCellExperiment} (typically via
\code{zellkonverter::readH5AD(signature_h5ad_path)}) rather than a file path, so this
function stays testable/portable — callers control exactly how/when the
(potentially large) real artifact is loaded, and tests can substitute a small
synthetic reference without needing a real \code{.h5ad} file at all. The pipeline
entrypoint (see the parent repository) is what resolves the real path via Python's
\code{signature_path()} contract and loads it before calling this.
}
\examples{
\donttest{
# BayesPrism's Gibbs sampling has real runtime (tens of seconds even on tiny
# data), so this is marked \donttest rather than run on every R CMD check. See
# tests/testthat/test-deconvolution.R for a real, executed run against a small
# synthetic reference with known ground-truth mixture proportions.
data(example_bulk_se)
# reference_sce would normally come from zellkonverter::readH5AD() on the real
# Phase 1 artifact; omitted here since building one from scratch needs several
# lines - see the test file for a complete worked construction.
}
}
