% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edger.R
\name{run_edger}
\alias{run_edger}
\title{Run edgeR differential expression}
\usage{
run_edger(se, condition_column, assay_name = "counts", alpha = 0.05)
}
\arguments{
\item{se}{A \code{SummarizedExperiment} with a raw-count assay.}

\item{condition_column}{Name of a two-level \code{colData(se)} column defining the
comparison groups (e.g. \code{"condition"}).}

\item{assay_name}{Which assay in \code{se} holds counts. Default \code{"counts"}.}

\item{alpha}{Significance threshold used for the \code{is_significant} column below
(\code{FDR < alpha}).}
}
\value{
A data frame (one row per gene) with edgeR's standard \code{topTags} columns
(\code{logFC}, \code{logCPM}, \code{F}, \code{PValue}, \code{FDR}) plus \code{is_significant}.

Non-integer counts (e.g. RSEM/Salmon-style estimated counts — see \code{ADR-0003}) are
rounded before use, matching \code{run_deseq2()}.
}
\description{
Wraps edgeR's standard \code{DGEList()} -> \code{calcNormFactors()} -> \code{estimateDisp()} ->
\code{glmQLFit()} -> \code{glmQLFTest()} quasi-likelihood workflow for a simple two-level
comparison — run on the \emph{same} count matrix \code{run_deseq2()} uses, so the two
methods' results are directly comparable (see \code{compare_de_methods()}).
}
\examples{
data(example_bulk_se)
res <- run_edger(example_bulk_se, condition_column = "condition")
head(res[order(res$FDR), ])
}
