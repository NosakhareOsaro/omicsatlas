% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_data.R
\name{read_matched_bulk_counts}
\alias{read_matched_bulk_counts}
\title{Load the GSE176078-matched bulk RNA-seq count matrix}
\usage{
read_matched_bulk_counts(counts_path)
}
\arguments{
\item{counts_path}{Path to the raw
\code{GSE176078_Wu_etal_2021_bulkRNAseq_raw_counts.txt} file: tab-separated, a
\code{Genes} column of gene symbols, then one column per patient ID (e.g.
\code{CID3586}) - the same patient IDs as the Phase 1 scRNA-seq signature's
\code{obs['orig.ident']}.}
}
\value{
A \code{SummarizedExperiment} with a \code{counts} assay (genes x samples) and
\code{rowData$gene_symbol}. No \code{colData} beyond sample names is set here - this
cohort has no single, sample-level tumour/normal split to build a DESeq2/edgeR
design from (every sample is tumour tissue); its role in this project is
feeding \code{\link[=run_bayesprism_deconvolution]{run_bayesprism_deconvolution()}}, not a two-group DE comparison. See
\code{docs/bulk-pipeline.md}.
}
\description{
Reads the raw, patient-matched bulk RNA-seq count matrix fetched by
\code{scripts/fetch_data.py} (\code{gse176078_bulk_matched} - see \code{ADR-0003}) into a
\code{SummarizedExperiment}. Values are RSEM-style non-integer estimated counts (not
literal raw counts, despite the source filename) - \code{\link[=run_deseq2]{run_deseq2()}}/\code{\link[=run_edger]{run_edger()}}
already round them.
}
\examples{
\donttest{
# Requires the real fetched file (scripts/fetch_data.py gse176078_bulk_matched).
se <- read_matched_bulk_counts("data/raw/bulk/gse176078_matched/GSE176078_Wu_etal_2021_bulkRNAseq_raw_counts.txt")
}
}
