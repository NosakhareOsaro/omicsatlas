% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deconvolution.R
\name{collapse_rare_cell_states}
\alias{collapse_rare_cell_states}
\title{Pool rare cell-type/cell-state combinations into a per-type "other" state}
\usage{
collapse_rare_cell_states(cell_type_labels, cell_state_raw, min_cells)
}
\arguments{
\item{cell_type_labels}{Character vector, one per cell.}

\item{cell_state_raw}{Character vector, one per cell (the raw \code{cell_state_col}
values, before combining with type).}

\item{min_cells}{Minimum cell count for a \code{cell_type}/\code{cell_state_raw} combination
to be kept as its own state.}
}
\value{
Character vector of final cell-state labels, one per cell.
}
\description{
\code{paste(cell_type, raw_state)} guarantees BayesPrism's required type/state nesting
(see \code{run_bayesprism_deconvolution()}) but, on the real Phase 1 signature,
produces many near-singleton combinations unsuited to stable expression-profile
estimation - see \code{ADR-0004} for the real state-size distribution this was measured
against and why \code{min_cells} defaults to \code{30} there. Combinations with fewer than
\code{min_cells} cells are replaced with a single \code{"<cell_type>_other"} label per type;
combinations at or above \code{min_cells} pass through as \code{"<cell_type>_<raw_state>"}
unchanged. Rarity is judged from each combination's total size across all cells
passed in, not recomputed after pooling.
}
\keyword{internal}
