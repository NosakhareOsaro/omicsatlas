% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment.R
\name{retry_with_backoff}
\alias{retry_with_backoff}
\title{Retry a call with exponential backoff}
\usage{
retry_with_backoff(
  fn,
  max_attempts = 3,
  delays = c(2, 8, 30),
  on_retry = message
)
}
\arguments{
\item{fn}{A zero-argument function to call.}

\item{max_attempts}{Maximum number of attempts, including the first. Default \code{3}.}

\item{delays}{Numeric vector of seconds to sleep between attempts; only the
first \code{max_attempts - 1} entries are used. Default \code{c(2, 8, 30)}.}

\item{on_retry}{Called with a single message string before each retry's sleep.
Default \code{\link[=message]{message()}}; tests pass a stub to capture/suppress this without
asserting on real console output.}
}
\value{
The result of the first successful call to \code{fn()}.
}
\description{
Small, generic helper for wrapping flaky external calls (currently just
\code{\link[=run_enrichment]{run_enrichment()}}'s live KEGG REST API call - see \code{ADR-0005}) in a bounded
number of retries, rather than either failing on the first transient error or
retrying silently/indefinitely.
}
\keyword{internal}
