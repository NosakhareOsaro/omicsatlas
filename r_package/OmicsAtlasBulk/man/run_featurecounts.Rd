% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantify.R
\name{run_featurecounts}
\alias{run_featurecounts}
\title{Quantify gene-level counts with featureCounts}
\usage{
run_featurecounts(bam_files, annotation_gtf, sample_names = NULL, ...)
}
\arguments{
\item{bam_files}{Character vector of BAM file paths.}

\item{annotation_gtf}{Path to a GTF annotation file.}

\item{sample_names}{Optional character vector naming each BAM file's sample (used
as the result's column names); defaults to the BAM file basenames with \code{.bam}/
\code{.BAM} stripped.}

\item{...}{Additional arguments passed through to \code{Rsubread::featureCounts()}
(e.g. \code{isPairedEnd}, \code{GTF.featureType}, \code{GTF.attrType}).}
}
\value{
A \code{SummarizedExperiment} with the gene-by-sample count matrix in
\code{assays$counts} and featureCounts' per-gene annotation (\code{Length}, etc.) in
\code{rowData}.
}
\description{
Wraps \code{Rsubread::featureCounts()} (Bioconductor, in-process — no external binary,
unlike \code{\link[=run_star_align]{run_star_align()}}; see \code{ADR-0004}) to quantify aligned reads against a GTF
annotation, returning a \code{SummarizedExperiment} rather than featureCounts' native
list-of-matrices output, matching this package's \code{SummarizedExperiment}-based
public API convention.
}
\examples{
# A real, small BAM + matching GTF are bundled with the package (built once via
# Rsubread::align() against a tiny synthetic reference — alignment itself has a
# ~3-minute fixed per-call overhead in Rsubread regardless of input size, so
# rebuilding it on every example/test run isn't practical; the *result* is
# real and bundled instead, and featureCounts() itself runs on it directly here,
# not stubbed).
bam <- system.file("extdata", "toy_aligned.bam", package = "OmicsAtlasBulk")
gtf <- system.file("extdata", "toy_annotation.gtf", package = "OmicsAtlasBulk")
se <- run_featurecounts(bam, gtf, sample_names = "toy_sample", isPairedEnd = FALSE)
SummarizedExperiment::assay(se, "counts")
}
